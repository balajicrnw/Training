void main(){
  const double pi=3.14; // const variables must be initialized at compile time and cannot be changed at runtime.
  final String city;
  city='Chennai';
  var name="Balaji";
  //name=12;  => once a variable is assigned a value of a specific type, it cannot be reassigned to a value of a different type.
  dynamic age=21;
  age="Twenty one"; // dynamic allows us to change the type of value assigned to it.
  //city='trichy'; final variables cannot be reassigned. can initial value later but once assigned it cannot be changed.
  //pi=12; const variables cannot be reassigned.


} 