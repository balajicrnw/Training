name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.8'
      SERVERPOD_PASSWORD_database: '_0bN-Gkl9jzUabf933YA5g_V8zWVAjGx'
      SERVERPOD_PASSWORD_redis: 'LWjpKMxXYZBZ1APOHL4W1yx-Tm1ht1AP'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: namma_kadai_backend_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: namma_kadai_backend_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: namma_kadai_backend_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: namma_kadai_backend_server
        run: serverpod generate

      - name: Run tests
        working-directory: namma_kadai_backend_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: namma_kadai_backend_server
        run: docker compose down -v
